- при нажатии кнопки "следить" нужно обновлять не только state приложения,
но и класть данные в localStorage для доступности background скриптом
Для этого в settings редьюсере нужно прописать сохранения для actions выбора клиники, специ
ализации, доктора - и сохранять это в localstorage

- functions и background - проверить, можно ли их переписать с es6

------------

1. Общие модули (которые будут использоваться и на фронте и в фоне)
    надо складывать в путь js/modules/common
2. К общим модулям относятся:
    - Хранилище настроек парсера и слежения (обёртка над chrome.storage.local)
    - Request и Parser (и с фронта запросы надо отправлять и с фона для слежения)

3. В будущем - модуль "сообщений" - общение между фоном и приложением.
    Например сообщения "начать слежение", "остановить слежение". МОжет ещё чего-нибудь придумается
